server:
  port: ${HANDYMAN_PORT}

grpc:
  server:
    port: ${GRPC_PORT}

management:
  endpoints:
    web:
      exposure:
        include: info, prometheus
      base-path:
      path-mapping:
        prometheus: /metrics
        info: /actuator/info
